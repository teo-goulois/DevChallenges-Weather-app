import React from "react";

const SearchButton = () => {
  return (
    <button type="submit" className="bg-blue-btn px-4 py-2 text-primary">
      Search
    </button>
  );
};

export default SearchButton;
